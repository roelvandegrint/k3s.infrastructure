apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rabbitmq-cluster-operator
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  sources:
    - repoURL: https://charts.bitnami.com/bitnami
      targetRevision: 4.3.20
      chart: rabbitmq-cluster-operator
      helm:
        valueFiles:
          - $values/rabbitmq-cluster-operator/values.yaml

    - repoURL: https://enecomanagedcloud@dev.azure.com/enecomanagedcloud/Myriad%20-%20VPP/_git/VPP.GitOps
      targetRevision: rabbitmq
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: rabbitmq-cluster-operator-system
