apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
 name: alertmanager.roelvandegrint.com-ingress
 annotations:
  traefik.ingress.kubernetes.io/router.entrypoints: web
  # cert-manager.io/issuer: letsencrypt-staging-http
spec:
  # tls:
  # - hosts:
  #   - alertmanager.roelvandegrint.com
  #   secretName: alertmanager-ingress-http-staging
  rules:
  - host: alertmanager.roelvandegrint.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: alertmanager-main
            port:
              number: 9093
