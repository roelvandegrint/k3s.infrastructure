apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
 name: prometheus.roelvandegrint.com-ingress
 annotations:
  traefik.ingress.kubernetes.io/router.entrypoints: websecure
  cert-manager.io/issuer: letsencrypt-staging-http
spec:
 tls:
   - hosts:
       - prometheus.roelvandegrint.com
     secretName: prometheus-ingress-http-staging
 rules:
   - host: prometheus.roelvandegrint.com
     http:
       paths:
         - path: /
           pathType: Prefix
           backend:
             service:
               name: prometheus-k8s
               port:
                 number: 9090
